# @macgrading/mobile

Team-only Expo app for minting MAC Grading certifications.

Run on the iOS simulator (from the repo root, with the API + docker stack running):

    EXPO_PUBLIC_DEV_AUTH=true pnpm --filter @macgrading/mobile ios

Full setup (env vars, dev sign-in, API) is documented in the repo root README.
